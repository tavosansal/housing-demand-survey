<div class="home-page">
    <div class="header"></div>
    <div class="banner-img">
        <div class="inner">
            {{#paper-card as |card|}}
                {{#card.title as |title|}}
                    {{#title.text as |text|}}
                        {{#text.headline}}Bismarck Housing Demand Survey{{/text.headline}}
                    {{/title.text}}
                {{/card.title}}
                {{#card.actions}}
                    {{#paper-button}}Learn More{{/paper-button}}
                    {{#paper-link 'demographics'}}
                      Begin
                    {{/paper-link}}
                {{/card.actions}}
            {{/paper-card}}
        </div>
    </div>
    <div class="footer">
        ©2017 Bismarck Infill Housing Coalition
    </div>
</div>
