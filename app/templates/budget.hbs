{{#survey-frame
  showSidebar=true
  totalSquareFootage=totalSquareFootage
  totalBudget=totalBudget
  costPerSquareFoot=costPerSquareFoot}}
    <div class="survey-nav">
        <div class="nav-section-wrapper">
            {{#link-to 'demographics' class="nav-section section-end"}}
                Demographics
            {{/link-to}}
            {{#link-to 'budget' class="nav-section active"}}
                Budget
            {{/link-to}}
            {{#link-to 'location' class="nav-section disabled"}}
                Location
            {{/link-to}}
            {{#link-to 'housing-type' class="nav-section disabled section-end"}}
                Housing Type
            {{/link-to}}
        </div>
        <div class="nav-section-wrapper">
            {{#link-to 'housing-size' class="nav-section disabled"}}
                Unit Plan
            {{/link-to}}
            {{#link-to 'housing-quality' class="nav-section disabled"}}
                Quality
            {{/link-to}}
            {{#link-to 'amenities' class="nav-section disabled"}}
                Amenities
            {{/link-to}}
        </div>
    </div>
    <div class="survey-header">
        <h1>Housing Cost Calculator</h1>
    </div>
    <div class="survey-body">
        <div class="budget">
            <h3>How many people live in your household?</h3>
            <div class="layout layout-align-center-center slider-container">
              {{paper-slider class="flex" min=1 max=7 value=peopleInHousehold onChange=(action (mut peopleInHousehold))}}
              {{paper-input type="number" min=1 value=peopleInHousehold onChange=(action (mut peopleInHousehold))}}
            </div>

            <h3>How many incomes are in your household?</h3>
            <div class="layout layout-align-center-center slider-container">
              {{paper-slider class="flex" min=1 max=7 value=incomesInHousehold onChange=(action (mut incomesInHousehold))}}
              {{paper-input type="number" value=incomesInHousehold onChange=(action (mut incomesInHousehold))}}
            </div>

            <h3 class="inline-block">What is your personal housing budget?</h3>
            <div class="help-hint">
                {{paper-icon "help" size=18}}
                <div class="popup bottom">
                    A good rule of thumb is not to spend over 30% on gross income on housing, or over 50% on a
                    combination of housing ant transportation.
                </div>
            </div>
            <div class="layout layout-align-center-center slider-container">
              {{paper-slider class="flex" min=300 max=2500 step=100 value=housingBudget onChange=(action (mut housingBudget))}}
              {{paper-input type="number" value=housingBudget onChange=(action (mut housingBudget))}}
            </div>

            <h3>How much sq. footage would you like to have per person?</h3>
            <div class="layout layout-align-center-center slider-container">
              {{paper-slider class="flex" min=300 max=1000 step=50 value=squareFeetPerPerson onChange=(action (mut squareFeetPerPerson))}}
              {{paper-input type="number" value=squareFeetPerPerson onChange=(action (mut squareFeetPerPerson))}}
            </div>
        </div>
    </div>
    <div class="survey-footer">
        {{#paper-button raised=true class='back-btn'}}
            {{#link-to 'demographics'}}Back{{/link-to}}
        {{/paper-button}}
        {{#paper-button raised=true primary=true class='next-btn' onClick=(action "continue")}}
          Continue
        {{/paper-button}}
    </div>
{{/survey-frame}}
